# SQLite for local development. Switch the datasource provider in prisma/schema.prisma
# to "postgresql" and point this at Postgres for production.
DATABASE_URL="file:./dev.db"
PORT=4000
HOST=0.0.0.0
# Comma-separated origins allowed by CORS
CORS_ORIGIN=http://localhost:5173
